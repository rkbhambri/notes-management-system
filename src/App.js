import React from 'react';
import Layout from './components/Layout/Layout';
import './App.css';

const App = (props) => {
    return (
        <div className="App">
            <Layout />
        </div>
    );
};

export default App;
